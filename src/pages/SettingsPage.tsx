import { Box, Typography } from "@mui/material";

const SettingsPage = () => {
  return (
    <Box paddingTop={10} paddingBottom={40}>
      <Typography fontFamily="inherit">Settings</Typography>
    </Box>
  );
};

export default SettingsPage;
